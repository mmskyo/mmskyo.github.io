---
title: favorites
---
---
## 즐겨찾기 기능 복습 노트

---

### 🧱 먼저 큰 그림 (레고 비유)

즐겨찾기 기능은 **4개의 레이어**로 구성돼 있어요. 각 레이어는 딱 자기 역할만 합니다.

```
👤 사용자가 버튼 누름
       ↓
🖥️  Fragment      → 화면 담당. 버튼 클릭 감지
       ↓
🧠  ViewModel     → 두뇌. "뭘 해야 하지?" 판단
       ↓
📦  Repository    → 실무자. 서버/DB에 실제로 일 시킴
       ↓
🗄️  DB / 서버    → 데이터 저장소
```

> **핵심 규칙**: 각 레이어는 바로 아래 레이어하고만 대화해요. Fragment가 DB를 직접 건드리면 ❌

---

### 🗄️ LAYER 1 — 데이터 저장소

#### Room DB (로컬 창고)

폰 안에 있는 미니 데이터베이스예요.

```kotlin
@Entity(tableName = "favorites_cache")
data class FavoriteCacheEntity(
    @PrimaryKey val favoriteId: String,  // 서버가 준 ID
    val url: String,
    val title: String?,
    val riskLevel: String,
    val addedAt: String
)
```

> **왜 로컬 DB에 저장해?** 서버 없이도 즐겨찾기 목록을 빠르게 보여주려고. 앱 켤 때마다 서버 기다리면 느리니까.

#### DAO (창고 직원)

DB에 명령 내리는 인터페이스예요.

```kotlin
@Dao
interface FavoriteCacheDao {
    @Query("SELECT * FROM favorites_cache ORDER BY addedAt DESC")
    fun getAll(): Flow<List<FavoriteCacheEntity>>   // 목록 구독

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insert(entity: FavoriteCacheEntity) // 추가

    @Query("DELETE FROM favorites_cache WHERE url = :url")
    suspend fun deleteByUrl(url: String)            // 삭제

    @Query("SELECT * FROM favorites_cache WHERE url = :url LIMIT 1")
    suspend fun getByUrl(url: String): FavoriteCacheEntity? // 조회
}
```

> **Flow가 뭐야?** "데이터가 바뀔 때마다 자동으로 알려줘" 하는 구독 시스템. 유튜브 알림 구독이랑 같은 개념이에요.

---

### 📦 LAYER 2 — Repository (실무 담당자)

서버 API 호출 + 로컬 DB 저장을 한 곳에서 처리해요.

```kotlin
@Singleton
class BookmarkRepository @Inject constructor(
    private val apiService: ApiService,
    private val favoriteCacheDao: FavoriteCacheDao
) {
```

**메서드 3개만 기억하면 됩니다:**

```kotlin
// 1. 서버 → 로컬 DB 동기화
suspend fun syncFromServer() {
    val response = apiService.getBookmarks()       // 서버에 요청
    if (response.isSuccessful) {
        val entities = response.body()?.items?.map { ... }
        favoriteCacheDao.clearAll()                // 기존 삭제
        favoriteCacheDao.insertAll(entities)       // 새로 저장
    }
}

// 2. 즐겨찾기 추가 (서버 먼저, 그 다음 로컬)
suspend fun add(url: String, title: String?, riskLevel: String) {
    val response = apiService.addBookmark(AddBookmarkRequest(url, title))
    if (response.isSuccessful) {
        val bookmarkId = response.body()!!.bookmarkId  // 서버 ID 받기
        favoriteCacheDao.insert(FavoriteCacheEntity(favoriteId = bookmarkId, ...))
    }
}

// 3. 즐겨찾기 삭제 (로컬 ID로 서버 ID 찾아서 삭제)
suspend fun remove(url: String) {
    val entity = favoriteCacheDao.getByUrl(url) ?: return
    apiService.deleteBookmark(entity.favoriteId)  // 서버 삭제
    favoriteCacheDao.deleteByUrl(url)             // 로컬 삭제
}
```

> **왜 서버 먼저야?** 서버가 실패하면 로컬에도 저장하면 안 되니까. 반대로 하면 서버 실패해도 로컬엔 있어서 데이터가 어긋남.

---

### 🧠 LAYER 3 — ViewModel (두뇌)

UI 상태를 관리하고 Repository에 일을 시켜요.

#### FavoritesViewModel (목록 화면용)

```kotlin
@HiltViewModel
class FavoritesViewModel @Inject constructor(
    private val bookmarkRepository: BookmarkRepository
) : ViewModel() {

    // Flow → StateFlow 변환 (화면이 구독할 수 있게)
    val favorites: StateFlow<List<FavoriteCacheEntity>> =
        bookmarkRepository.getAll()
            .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())

    init {
        // 화면 처음 열릴 때 서버 동기화
        viewModelScope.launch { bookmarkRepository.syncFromServer() }
    }

    fun delete(url: String) {
        viewModelScope.launch { bookmarkRepository.remove(url) }
    }
}
```

#### ResultViewModel (스캔 결과 화면용)

```kotlin
@HiltViewModel
class ResultViewModel @Inject constructor(
    private val bookmarkRepository: BookmarkRepository
) : ViewModel() {

    private val _isFavorite = MutableStateFlow(false)
    val isFavorite = _isFavorite.asStateFlow()

    // 현재 URL이 즐겨찾기인지 확인
    fun checkFavorite(url: String) {
        viewModelScope.launch {
            _isFavorite.value = bookmarkRepository.isFavorite(url)
        }
    }

    // 즐겨찾기 토글 (있으면 삭제, 없으면 추가)
    fun toggleFavorite(url: String, title: String?, riskLevel: String) {
        viewModelScope.launch {
            if (_isFavorite.value) bookmarkRepository.remove(url)
            else bookmarkRepository.add(url, title, riskLevel)
            _isFavorite.value = !_isFavorite.value
        }
    }
}
```

> **StateFlow가 뭐야?** ViewModel이 가진 "현재 상태"예요. Fragment가 구독하면 값이 바뀔 때마다 자동으로 화면이 업데이트돼요.

---

### 🖥️ LAYER 4 — Fragment (화면)

ViewModel을 구독하고, 사용자 액션을 ViewModel에 전달해요.

```kotlin
// ViewModel 연결
private val viewModel: FavoritesViewModel by viewModels()

override fun onViewCreated(...) {
    // RecyclerView 설정
    binding.rvFavorites.adapter = favoritesAdapter
    binding.rvFavorites.layoutManager = LinearLayoutManager(requireContext())

    // ViewModel 구독 → 데이터 바뀌면 자동 업데이트
    viewLifecycleOwner.lifecycleScope.launch {
        viewLifecycleOwner.repeatOnLifecycle(Lifecycle.State.STARTED) {
            viewModel.favorites.collect { list ->
                if (list.isEmpty()) {
                    binding.layoutEmpty.visibility = View.VISIBLE
                    binding.rvFavorites.visibility = View.GONE
                } else {
                    binding.layoutEmpty.visibility = View.GONE
                    binding.rvFavorites.visibility = View.VISIBLE
                    favoritesAdapter.submitList(list)
                }
            }
        }
    }
}
```

---

### 🔄 전체 흐름 한눈에 보기

**즐겨찾기 탭 열 때:**

```
Fragment 열림
→ ViewModel init 실행
→ bookmarkRepository.syncFromServer()
→ 서버 GET 요청
→ 응답 받아서 로컬 DB에 저장
→ DB가 Flow로 변경 알림
→ StateFlow 업데이트
→ Fragment의 collect 실행
→ 화면 자동 갱신
```

**즐겨찾기 추가할 때:**

```
사용자가 ★ 버튼 클릭
→ Fragment: viewModel.toggleFavorite(url, title, riskLevel)
→ ViewModel: bookmarkRepository.add()
→ Repository: 서버 POST 요청
→ 서버가 bookmarkId 응답
→ 로컬 DB insert
→ Flow 자동 방출
→ 화면 자동 갱신
```

---

### 🔑 처음부터 구현할 때 순서

혼자 만들 때 이 순서대로 하면 됩니다:

```
1. Entity 만들기         → DB에 뭘 저장할지 정의
2. DAO 만들기            → DB 명령어 작성
3. AppDatabase 등록      → entities에 추가
4. Repository 만들기     → API + DAO 연결
5. ViewModel 만들기      → StateFlow + 함수
6. Fragment 만들기       → collect + 어댑터 연결
7. 레이아웃 XML 만들기   → 화면 구성
8. 네비게이션 등록       → nav_graph.xml
```

> **막힐 때 체크리스트**
> 
> - Flow가 안 오면? → DAO 반환 타입이 `Flow<>`인지 확인
> - 화면이 안 바뀌면? → `repeatOnLifecycle` 빠진 건 아닌지 확인
> - 서버 요청이 안 가면? → AuthInterceptor가 토큰 붙이는지 Logcat 확인
> - 삭제가 서버에 안 되면? → `favoriteId`가 서버에서 받은 ID인지 확인


# 더미 데이터 결과화면 꼭 나오게하기
# 북마크 기능 버그 수정 + 더미 데이터 fallback 추가

## 배경 (왜 이 작업이 필요한가?)

### 상황

- 서버 연동 북마크(추가/삭제) 테스트가 필요하다

- 북마크 UI는 **스캔 결과 화면**(ResultSecured/Blocked/Whitelist Fragment)에 있다

- 그런데 ML 모델 연동이 아직 안 돼서, 서버가 스캔 결과를 줄 수 없다

- 결과 화면 자체에 진입을 못 하니 북마크 테스트도 불가능한 상황

### 해결 전략

```

[스캔 분석] 서버 실패 → 더미 ScanResult로 대체 → 결과 화면 진입 가능

[북마크 CRUD] 실제 서버 API 호출 → 추가/삭제 테스트 가능

```

즉, **스캔은 가짜(더미)**, **북마크는 진짜(서버)**로 분리 동작시킨다.

---

## 전체 데이터 흐름 이해

### 정상 흐름 (모델 연동 후)

```

QR 스캔 → ScanViewModel.analyzeUrl(url)

├─ 1순위: Room DB 화이트리스트 히트? → ResultWhitelistFragment (로컬)

├─ 2순위: Room DB 즐겨찾기 히트? → ResultWhitelistFragment (로컬)

└─ 3순위: 서버 POST /api/v1/scan/url → ScanningFragment(로딩)

└─ 응답 → ResultSecuredFragment 또는 ResultBlockedFragment

```

### 지금 흐름 (모델 미연동)

```

QR 스캔 → 서버 호출 → 실패 (모델 없음) → ❌ Error 상태 → 결과 화면 못 감

```

### 수정 후 흐름 (더미 fallback)

```

QR 스캔 → 서버 호출 → 실패 → ✅ 더미 ScanResult 반환 → 결과 화면 진입

└─ 결과 화면에서 별 버튼 → POST/DELETE /api/v1/users/me/bookmarks (실제 서버)

```

---

## 발견된 버그 목록

| # | 심각도 | 파일 | 문제 |

|---|--------|------|------|

| 1 | **CRITICAL** | `ResultViewModel.kt` | `toggleFavorite()`에서 서버 실패해도 UI 상태를 무조건 반전 (`add()/remove()` 반환값 무시) |

| 2 | **MEDIUM** | `ResultSecuredFragment.kt` | 레이아웃에 `btn_favorite` 있는데 클릭 리스너 없음 (죽은 버튼) |

| 3 | **MEDIUM** | `ResultBlockedFragment.kt` | 북마크 버튼 자체가 레이아웃에 없고, viewModel 미사용 |

| 4 | **MEDIUM** | `ResultWhitelistFragment.kt` | riskLevel을 항상 `"SECURED"`로 하드코딩 |

| 5 | **MEDIUM** | `ResultSecuredFragment.kt` | 툴바 뒤로가기가 `action_result_whitelist_to_scan` 사용 (다른 Fragment의 액션) |

| 6 | **MEDIUM** | `ResultBlockedFragment.kt` | 툴바 뒤로가기가 `action_result_whitelist_to_scan` 사용 (다른 Fragment의 액션) |

---

## Step 1: ResultViewModel — toggleFavorite 에러 처리 수정

**파일**: `app/src/main/java/com/qguarder/android/ui/result/ResultViewModel.kt`

**문제**: 서버 호출 실패해도 `_isFavorite.value = !_isFavorite.value`로 UI 상태를 무조건 뒤집음

**수정 전 (버그 있는 코드)**:

```kotlin

fun toggleFavorite(url: String, title: String?, riskLevel: String) {

viewModelScope.launch {

if (_isFavorite.value) {

bookmarkRepository.remove(url) // ← 반환값 없음, 실패해도 모름

} else {

bookmarkRepository.add(url, title, riskLevel) // ← Boolean 반환하는데 무시

}

_isFavorite.value = !_isFavorite.value // ← 무조건 반전 (서버 실패해도!)

}

}

```

**수정 후**:

```kotlin

fun toggleFavorite(url: String, title: String?, riskLevel: String) {

viewModelScope.launch {

try {

if (_isFavorite.value) {

bookmarkRepository.remove(url)

// remove()는 서버 성공 시에만 로컬 삭제하므로, 로컬 DB 재확인

_isFavorite.value = favoriteRepository.isFavorite(url)

} else {

val success = bookmarkRepository.add(url, title, riskLevel)

if (success) _isFavorite.value = true

// 실패하면 상태 안 바꿈 → UI가 원래대로 유지됨

}

} catch (_: Exception) {

// 네트워크 에러 시 아무것도 안 함 → UI 상태 유지

}

}

}

```

**핵심 포인트**:

- `add()`는 `Boolean`을 반환 → `true`일 때만 상태 변경

- `remove()`는 반환값이 없지만 서버 성공 시에만 로컬 DB를 삭제 → `isFavorite()` 재확인으로 검증

- `try-catch`로 네트워크 에러 방어

---

## Step 2: ResultSecuredFragment — 북마크 버튼 연결

**파일**: `app/src/main/java/com/qguarder/android/ui/result/ResultSecuredFragment.kt`

**문제**: 레이아웃(`fragment_result_secured.xml`)에 `btn_favorite` ImageButton이 이미 있지만, Fragment 코드에 클릭 리스너가 없어서 눌러도 아무 반응 없음

**수정 후 전체 코드**:

```kotlin

package com.qguarder.android.ui.result

import android.os.Bundle

import android.view.LayoutInflater

import android.view.View

import android.view.ViewGroup

import androidx.fragment.app.Fragment

import androidx.fragment.app.viewModels

import androidx.lifecycle.Lifecycle

import androidx.lifecycle.lifecycleScope

import androidx.lifecycle.repeatOnLifecycle

import androidx.navigation.fragment.findNavController

import com.qguarder.android.R

import com.qguarder.android.data.local.CustomTabsHelper

import com.qguarder.android.databinding.FragmentResultSecuredBinding

import dagger.hilt.android.AndroidEntryPoint

import kotlinx.coroutines.launch

import kotlin.getValue

@AndroidEntryPoint

class ResultSecuredFragment : Fragment() {

private var _binding: FragmentResultSecuredBinding? = null

private val binding get() = _binding!!

private val viewModel: ResultViewModel by viewModels()

override fun onCreateView(

inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?

): View {

_binding = FragmentResultSecuredBinding.inflate(inflater, container, false)

return binding.root

}

private var currentUrl: String = ""

override fun onViewCreated(view: View, savedInstanceState: Bundle?) {

super.onViewCreated(view, savedInstanceState)

currentUrl = arguments?.getString("url") ?: ""

// 툴바 이전버튼 → 스캔 화면

// (기존 버그: action_result_whitelist_to_scan 사용 → secured 전용 액션으로 수정)

binding.toolbar.setNavigationOnClickListener {

findNavController().navigate(R.id.action_result_secured_to_scan)

}

// ── 북마크 기능 (추가된 부분) ──────────────────────

// 1) 화면 진입 시 이 URL이 이미 북마크인지 확인

if (currentUrl.isNotEmpty()) {

viewModel.checkFavorite(currentUrl)

}

// 2) 별 버튼 클릭 → 북마크 추가/삭제 토글

binding.btnFavorite.setOnClickListener {

viewModel.toggleFavorite(

url = currentUrl,

title = null,

riskLevel = "SECURED"

)

}

// 3) ViewModel의 isFavorite 상태를 관찰 → 별 아이콘 변경

viewLifecycleOwner.lifecycleScope.launch {

repeatOnLifecycle(Lifecycle.State.STARTED) {

viewModel.isFavorite.collect { isFavorite ->

binding.btnFavorite.setImageResource(

if (isFavorite) R.drawable.ic_favorites // 채워진 별

else R.drawable.ic_star_outline // 빈 별

)

}

}

}

// ── 북마크 기능 끝 ─────────────────────────────────

// 돌아가기

binding.btnBack.setOnClickListener {

findNavController().navigate(R.id.action_result_secured_to_scan)

}

// 웹사이트 연결

binding.btnConnect.setOnClickListener {

CustomTabsHelper.open(requireContext(), currentUrl)

}

}

override fun onDestroyView() { super.onDestroyView(); _binding = null }

}

```

**변경 포인트 정리**:

- import 추가: `Lifecycle`, `lifecycleScope`, `repeatOnLifecycle`, `kotlinx.coroutines.launch`

- `viewModel.checkFavorite(currentUrl)` — 진입 시 초기 상태 확인

- `btnFavorite.setOnClickListener` — 별 버튼에 토글 기능 연결

- `isFavorite.collect` — StateFlow 관찰로 별 아이콘 실시간 반영

- 툴바 뒤로가기 액션 수정: `action_result_whitelist_to_scan` → `action_result_secured_to_scan`

---

## Step 3: ResultBlockedFragment — 북마크 버튼 추가

### 3-1. 레이아웃 수정

**파일**: `app/src/main/res/layout/fragment_result_blocked.xml`

**문제**: Toolbar에 `btn_favorite` 버튼이 없음 (Secured 레이아웃에는 있음)

**수정**: Toolbar 닫는 태그 직전에 ImageButton 추가

```xml

<androidx.appcompat.widget.Toolbar

android:id="@+id/toolbar"

android:layout_width="match_parent"

android:layout_height="?attr/actionBarSize"

app:navigationIcon="@drawable/ic_arrow_back"

app:title="">

<!-- ★ 추가: 북마크 버튼 -->

<ImageButton

android:id="@+id/btn_favorite"

android:layout_width="40dp"

android:layout_height="40dp"

android:layout_gravity="end"

android:layout_marginEnd="8dp"

android:src="@drawable/ic_star_outline"

android:background="?attr/selectableItemBackgroundBorderless"

android:padding="8dp"

android:contentDescription="즐겨찾기" />

</androidx.appcompat.widget.Toolbar>

```

### 3-2. Fragment 코드 수정

**파일**: `app/src/main/java/com/qguarder/android/ui/result/ResultBlockedFragment.kt`

**수정 후 전체 코드**:

```kotlin

package com.qguarder.android.ui.result

import android.os.Bundle

import android.view.LayoutInflater

import android.view.View

import android.view.ViewGroup

import androidx.fragment.app.Fragment

import androidx.fragment.app.viewModels

import androidx.lifecycle.Lifecycle

import androidx.lifecycle.lifecycleScope

import androidx.lifecycle.repeatOnLifecycle

import androidx.navigation.fragment.findNavController

import com.qguarder.android.R

import com.qguarder.android.data.local.CustomTabsHelper

import com.qguarder.android.databinding.FragmentResultBlockedBinding

import dagger.hilt.android.AndroidEntryPoint

import kotlinx.coroutines.launch

@AndroidEntryPoint

class ResultBlockedFragment : Fragment() {

private var _binding: FragmentResultBlockedBinding? = null

private val binding get() = _binding!!

private val viewModel: ResultViewModel by viewModels()

override fun onCreateView(

inflater: LayoutInflater, container: ViewGroup?, savedInstanceState: Bundle?

): View {

_binding = FragmentResultBlockedBinding.inflate(inflater, container, false)

return binding.root

}

private var currentUrl: String = ""

override fun onViewCreated(view: View, savedInstanceState: Bundle?) {

super.onViewCreated(view, savedInstanceState)

currentUrl = arguments?.getString("url") ?: ""

// 툴바 이전버튼 → 스캔 화면

// (기존 버그: action_result_whitelist_to_scan 사용 → blocked 전용 액션으로 수정)

binding.toolbar.setNavigationOnClickListener {

findNavController().navigate(R.id.action_result_blocked_to_scan)

}

// ── 북마크 기능 (추가된 부분) ──────────────────────

// 1) 화면 진입 시 이 URL이 이미 북마크인지 확인

if (currentUrl.isNotEmpty()) {

viewModel.checkFavorite(currentUrl)

}

// 2) 별 버튼 클릭 → 북마크 추가/삭제 토글

binding.btnFavorite.setOnClickListener {

viewModel.toggleFavorite(

url = currentUrl,

title = null,

riskLevel = "BLOCKED" // ← Secured와 다른 점: riskLevel이 "BLOCKED"

)

}

// 3) ViewModel의 isFavorite 상태를 관찰 → 별 아이콘 변경

viewLifecycleOwner.lifecycleScope.launch {

repeatOnLifecycle(Lifecycle.State.STARTED) {

viewModel.isFavorite.collect { isFavorite ->

binding.btnFavorite.setImageResource(

if (isFavorite) R.drawable.ic_favorites

else R.drawable.ic_star_outline

)

}

}

}

// ── 북마크 기능 끝 ─────────────────────────────────

// 돌아가기

binding.btnBack.setOnClickListener {

findNavController().navigate(R.id.action_result_blocked_to_scan)

}

// 무시하고 접속

binding.btnIgnore.setOnClickListener {

android.app.AlertDialog.Builder(requireContext())

.setTitle("위험 경고")

.setMessage("이 URL은 피싱 사이트로 탐지되었습니다.\n정말 접속하시겠습니까?")

.setPositiveButton("접속하기") { _, _ ->

CustomTabsHelper.open(requireContext(), currentUrl)

}

.setNegativeButton("취소", null)

.show()

}

}

override fun onDestroyView() { super.onDestroyView(); _binding = null }

}

```

**Secured와의 차이점**:

- `riskLevel = "BLOCKED"` (Secured는 `"SECURED"`)

- 레이아웃에 `btn_favorite` 추가 필요 (Secured는 이미 있음)

- "무시하고 접속" 버튼 로직 유지

---

## Step 4: UrlRepository — 더미 데이터 fallback

**파일**: `app/src/main/java/com/qguarder/android/data/remote/UrlRepository.kt`

**수정 전**:

```kotlin

// 2. 서버 분석

return try {

val resp = apiService.scanUrl(ScanRequest(url = url, source = source))

if (!resp.isSuccessful || resp.body() == null) {

Result.failure(Exception("분석 실패: ${resp.code()}")) // ← 실패 시 에러

} else {

Result.success(resp.body()!!.toDomain())

}

} catch (e: Exception) {

Result.failure(e) // ← 네트워크 에러 시 에러

}

```

**수정 후**:

```kotlin

// 2. 서버 분석

return try {

val resp = apiService.scanUrl(ScanRequest(url = url, source = source))

if (!resp.isSuccessful || resp.body() == null) {

// TODO: 모델 연동 완료 후 삭제 — 테스트용 더미 결과

Result.success(dummyScanResult(url))

} else {

Result.success(resp.body()!!.toDomain())

}

} catch (e: Exception) {

// TODO: 모델 연동 완료 후 삭제 — 테스트용 더미 결과

Result.success(dummyScanResult(url))

}

```

**추가할 private 함수** (파일 하단):

```kotlin

// TODO: 모델 연동 완료 후 삭제

private fun dummyScanResult(url: String) = ScanResult(

scanId = "dummy-${System.currentTimeMillis()}",

url = url,

riskLevel = RiskLevel.SECURED,

finalScore = 0.15f,

vtTotalEngines = 90,

vtDetectedEngines = 0,

xaiFeatures = listOf(

XaiItem("url_length", "${url.length}", 0.05f),

XaiItem("has_https", if (url.startsWith("https")) "true" else "false", -0.10f),

),

scannedAt = java.time.Instant.now().toString(),

)

```

**나중에 모델 연동하면**: `TODO` 주석 달린 부분을 원래 `Result.failure(...)`로 되돌리고, `dummyScanResult()` 함수 삭제하면 됨

---

## 수정 파일 요약

| 파일 | 변경 내용 |

|------|-----------|

| `ResultViewModel.kt` | toggleFavorite 에러 처리 |

| `ResultSecuredFragment.kt` | 북마크 버튼 리스너 + 상태 관찰 + 네비게이션 액션 수정 |

| `ResultBlockedFragment.kt` | 북마크 버튼 리스너 + 상태 관찰 + 네비게이션 액션 수정 |

| `fragment_result_blocked.xml` | Toolbar에 btn_favorite 추가 |

| `UrlRepository.kt` | 서버 실패 시 더미 ScanResult fallback |

## 검증 방법

1. 앱 빌드 성공 확인

2. QR 스캔 → 서버 실패해도 더미 결과로 Secured 화면 진입 확인

3. Secured 화면에서 별 버튼 클릭 → 북마크 추가 (서버 API 호출 확인)

4. 다시 별 버튼 → 북마크 삭제 (서버 API 호출 확인)

5. Blocked 화면에서도 동일하게 동작 확인

6. 서버 에러 시 별 버튼 상태가 원래대로 유지되는지 확인

7. FavoritesFragment(즐겨찾기 목록)에서 추가/삭제 반영 확인